<!DOCTYPE html>
<html lang="{{ site.lang | default: "en-US" }}">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <title>{{ page.title }} - {{ site.title }}</title>
    <link rel="stylesheet" href="{{ "/assets/css/main.css" | relative_url }}">
  </head>
  <body>
    <div class="bg-neutral-50 sticky shadow-neutral-100 shadow-sm h-dvh flex items-center justify-center px-4">
        <img src="{{ "assets/images/logo-pixento.svg" | relative_url }}" alt="Pixento logo" class="max-w-80">
    </div>
    {{ content }}
  </body>
</html>
